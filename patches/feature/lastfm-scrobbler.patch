From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: keet <keetsta@icloud.com>
Date: Tue, 18 Aug 2026 01:14:37 +0800
Subject: Add Last.fm scrobbling

---
 submodules/TelegramUI/Sources/MediaManager.swift           | 2 ++
 submodules/TelegramUIPreferences/Sources/PostboxKeys.swift | 2 ++
 2 files changed, 4 insertions(+)

diff --git a/submodules/TelegramUI/Sources/MediaManager.swift b/submodules/TelegramUI/Sources/MediaManager.swift
index 0000000..0000000 100644
--- a/submodules/TelegramUI/Sources/MediaManager.swift
+++ b/submodules/TelegramUI/Sources/MediaManager.swift
@@ -131,6 +131,7 @@ public final class MediaManagerImpl: NSObject, MediaManager {
                 } else {
                     self.musicMediaPlayerStateValue.set(.single(nil))
                     self.musicListenTracker?.playerClosed()
+                    ClearScrobbler.shared.playerClosed()
                 }
             }
         }
@@ -436,6 +437,7 @@ public final class MediaManagerImpl: NSObject, MediaManager {
         self.musicListenTrackingDisposable.set((self.musicMediaPlayerState
         |> deliverOnMainQueue).startStrict(next: { [weak self] stateAndType in
             self?.musicListenTracker?.update(with: stateAndType)
+            ClearScrobbler.shared.update(state: stateAndType?.1, type: stateAndType?.2)
         }))
 
         self.globalAudioSessionForegroundDisposable.set((shouldKeepAudioSession |> deliverOnMainQueue).startStrict(next: { [weak self] value in
diff --git a/submodules/TelegramUIPreferences/Sources/PostboxKeys.swift b/submodules/TelegramUIPreferences/Sources/PostboxKeys.swift
index 0000000..0000000 100644
--- a/submodules/TelegramUIPreferences/Sources/PostboxKeys.swift
+++ b/submodules/TelegramUIPreferences/Sources/PostboxKeys.swift
@@ -55,6 +55,7 @@ private enum ApplicationSpecificSharedDataKeyValues: Int32 {
     case clearConfigSettings = 30
     case hiddenChatsSettings = 31
     case hiddenMessagesSettings = 32
+    case lastFmSettings = 33
 }
 
 public struct ApplicationSpecificSharedDataKeys {
@@ -85,6 +86,7 @@ public struct ApplicationSpecificSharedDataKeys {
     public static let clearConfigSettings: EngineDataBuffer = applicationSpecificSharedDataKey(ApplicationSpecificSharedDataKeyValues.clearConfigSettings.rawValue)
     public static let hiddenChatsSettings: EngineDataBuffer = applicationSpecificSharedDataKey(ApplicationSpecificSharedDataKeyValues.hiddenChatsSettings.rawValue)
     public static let hiddenMessagesSettings: EngineDataBuffer = applicationSpecificSharedDataKey(ApplicationSpecificSharedDataKeyValues.hiddenMessagesSettings.rawValue)
+    public static let lastFmSettings: EngineDataBuffer = applicationSpecificSharedDataKey(ApplicationSpecificSharedDataKeyValues.lastFmSettings.rawValue)
 }
 
 private enum ApplicationSpecificItemCacheCollectionIdValues: Int8 {
