From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: keet <keetsta@icloud.com>
Date: Thu, 13 Aug 2026 23:13:11 +0800
Subject: Send a gallery video as a video message

---
 .../TGPhotoEditorController.h                 |  8 +++
 .../Sources/TGPhotoAvatarPreviewController.h  |  3 ++
 .../Sources/TGPhotoAvatarPreviewController.m  |  1 +
 .../Sources/TGPhotoEditorController.m         | 26 ++++++----
 .../Sources/LegacyMediaPickers.swift          | 50 ++++++++++++++++---
 .../Sources/MediaPickerScreen.swift           | 25 ++++++++--
 6 files changed, 93 insertions(+), 20 deletions(-)

diff --git a/submodules/LegacyComponents/PublicHeaders/LegacyComponents/TGPhotoEditorController.h b/submodules/LegacyComponents/PublicHeaders/LegacyComponents/TGPhotoEditorController.h
index 0000000..0000000 100644
--- a/submodules/LegacyComponents/PublicHeaders/LegacyComponents/TGPhotoEditorController.h
+++ b/submodules/LegacyComponents/PublicHeaders/LegacyComponents/TGPhotoEditorController.h
@@ -28,6 +28,14 @@ typedef enum {
 
 @interface TGPhotoEditorController : TGOverlayController
 
+// ClearGram: the avatar intent hardcodes a 9.9s trim limit. Cleargram reuses this editor for
+// round video messages, which allow 60s, so the cap is a property. Default keeps avatars as-is.
+@property (nonatomic, assign) NSTimeInterval clearMaximumVideoDuration;
+
+// ClearGram: the avatar flow lets you pick a keyframe for the still profile photo (the dot on
+// the scrubber plus its zoom animation). A round video message has no such still, so hide it.
+@property (nonatomic, assign) bool clearHidesKeyframePicker;
+
 @property (nonatomic, strong) TGMediaEditingContext *editingContext;
 @property (nonatomic, strong) id<TGPhotoPaintStickersContext> stickersContext;
 
diff --git a/submodules/LegacyComponents/Sources/TGPhotoAvatarPreviewController.h b/submodules/LegacyComponents/Sources/TGPhotoAvatarPreviewController.h
index 0000000..0000000 100644
--- a/submodules/LegacyComponents/Sources/TGPhotoAvatarPreviewController.h
+++ b/submodules/LegacyComponents/Sources/TGPhotoAvatarPreviewController.h
@@ -18,6 +18,9 @@
 @property (nonatomic, copy) void (^croppingChanged)(void);
 @property (nonatomic, copy) void (^togglePlayback)(void);
 
+// ClearGram: hides the "select cover frame" hint — a round video message has no cover.
+@property (nonatomic, assign) bool clearHidesCoverLabel;
+
 @property (nonatomic, weak) UIView *dotImageView;
 @property (nonatomic, weak) UIView *dotMarkerView;
 @property (nonatomic, weak) PGPhotoEditorView *fullPreviewView;
diff --git a/submodules/LegacyComponents/Sources/TGPhotoAvatarPreviewController.m b/submodules/LegacyComponents/Sources/TGPhotoAvatarPreviewController.m
index 0000000..0000000 100644
--- a/submodules/LegacyComponents/Sources/TGPhotoAvatarPreviewController.m
+++ b/submodules/LegacyComponents/Sources/TGPhotoAvatarPreviewController.m
@@ -899,6 +899,7 @@ - (void)updateToolViews
             
             _portraitToolsWrapperView.frame = CGRectMake(screenEdges.left, screenEdges.bottom - panelToolbarPortraitSize, referenceSize.width, panelToolbarPortraitSize);
             
+            _coverLabel.hidden = self.clearHidesCoverLabel;
             _coverLabel.frame = CGRectMake(floor((_portraitToolsWrapperView.frame.size.width - _coverLabel.frame.size.width) / 2.0), CGRectGetMaxY(_scrubberView.frame) + 6.0, _coverLabel.frame.size.width, _coverLabel.frame.size.height);
             
             _titleLabel.frame = CGRectMake(screenEdges.left + floor((referenceSize.width - _titleLabel.frame.size.width) / 2.0), screenEdges.top + floor((44.0 - _titleLabel.frame.size.height) / 2.0), _titleLabel.frame.size.width, _titleLabel.frame.size.height);
diff --git a/submodules/LegacyComponents/Sources/TGPhotoEditorController.m b/submodules/LegacyComponents/Sources/TGPhotoEditorController.m
index 0000000..0000000 100644
--- a/submodules/LegacyComponents/Sources/TGPhotoEditorController.m
+++ b/submodules/LegacyComponents/Sources/TGPhotoEditorController.m
@@ -161,7 +161,9 @@ - (instancetype)initWithContext:(id<LegacyComponentsContext>)context item:(id<TG
         _item = item;
         _currentTab = selectedTab;
         _intent = intent;
-        
+        // ClearGram: stock's hardcoded avatar trim limit, now overridable (round video wants 60s).
+        _clearMaximumVideoDuration = 9.9;
+
         _caption = caption;
         _initialAdjustments = adjustments;
         _screenImage = screenImage;
@@ -386,7 +388,7 @@ - (void)loadView
         _scrubberView = [[TGMediaPickerGalleryVideoScrubber alloc] initWithFrame:CGRectMake(0.0f, 0.0, _portraitToolbarView.frame.size.width, 68.0f) cover: false];
         _scrubberView.minimumLength = 3.0;
         _scrubberView.layer.allowsGroupOpacity = true;
-        _scrubberView.hasDotPicker = true;
+        _scrubberView.hasDotPicker = !self.clearHidesKeyframePicker;
         _scrubberView.dataSource = self;
         _scrubberView.delegate = self;
         _scrubberView.clipsToBounds = false;
@@ -484,9 +486,9 @@ - (void)viewDidLoad
         _scrubberView.disableZoom = true;
         _scrubberView.disableTimeDisplay = true;
         _scrubberView.trimStartValue = 0.0;
-        _scrubberView.trimEndValue = MIN(9.9, self.item.originalDuration);
-        [_scrubberView setTrimApplied:self.item.originalDuration > 9.9];
-        _scrubberView.maximumLength = 9.9;
+        _scrubberView.trimEndValue = MIN(self.clearMaximumVideoDuration, self.item.originalDuration);
+        [_scrubberView setTrimApplied:self.item.originalDuration > self.clearMaximumVideoDuration];
+        _scrubberView.maximumLength = self.clearMaximumVideoDuration;
         
         [self setVideoEndTime:_scrubberView.trimEndValue];
     }
@@ -645,7 +647,7 @@ - (NSTimeInterval)trimEndValue {
         if (_scrubberView.trimEndValue > 0.0)
             return _scrubberView.trimEndValue;
         else
-            return MIN(9.9, _scrubberView.duration);
+            return MIN(self.clearMaximumVideoDuration, _scrubberView.duration);
     } else {
         return _photoEditor.trimEndValue;
     }
@@ -1383,6 +1385,7 @@ - (void)presentTab:(TGPhotoEditorTab)tab
                 TGPhotoAvatarPreviewController *cropController = [[TGPhotoAvatarPreviewController alloc] initWithContext:_context photoEditor:_photoEditor previewView:_previewView isForum:[self presentedForForumAvatarCreation] isSuggestion:[self presentedForSuggestedAvatar] isSuggesting:[self presentedForSuggestingAvatar] senderName:self.senderName];
                 cropController.stickersContext = _stickersContext;
                 cropController.scrubberView = _scrubberView;
+                cropController.clearHidesCoverLabel = self.clearHidesKeyframePicker;
                 cropController.dotImageView = _dotImageView;
                 cropController.dotMarkerView = _dotMarkerView;
                 cropController.fullPreviewView = _fullPreviewView;
@@ -2079,7 +2082,7 @@ - (void)applyEditor
     {
         videoStartValue = _dotPosition;
         trimStartValue = self.trimStartValue;
-        trimEndValue = MIN(self.trimStartValue + 9.9, self.trimEndValue);
+        trimEndValue = MIN(self.trimStartValue + self.clearMaximumVideoDuration, self.trimEndValue);
     }
     
     [self stopVideoPlayback:true];
@@ -2699,10 +2702,13 @@ - (void)videoScrubberDidBeginScrubbing:(TGMediaPickerGalleryVideoScrubber *)__un
     if (![previewController isKindOfClass:[TGPhotoAvatarPreviewController class]])
         return;
     
-    [previewController beginScrubbing:true];
+    // ClearGram: the cover label and the white confirm-flash belong to avatar keyframe picking.
+    [previewController beginScrubbing:!self.clearHidesKeyframePicker];
 }
 
 - (void)resetDotImage {
+    if (self.clearHidesKeyframePicker)
+        return;
     UIView *snapshotView = nil;
     UIView *dotSnapshotView = nil;
     if (_dotImageView.image != nil) {
@@ -2733,6 +2739,8 @@ - (void)resetDotImage {
 }
 
 - (void)updateDotImage:(bool)animated {
+    if (self.clearHidesKeyframePicker)
+        return;
     AVPlayer *player = _player;
     if (player == nil) {
         return;
@@ -2850,7 +2858,7 @@ - (void)videoScrubberDidEndScrubbing:(TGMediaPickerGalleryVideoScrubber *)videoS
     
     _dotPosition = videoScrubber.value;
     
-    [previewController endScrubbing:true completion:^bool{
+    [previewController endScrubbing:!self.clearHidesKeyframePicker completion:^bool{
         __strong TGPhotoEditorController *strongSelf = weakSelf;
         if (strongSelf == nil)
             return false;
diff --git a/submodules/LegacyMediaPickerUI/Sources/LegacyMediaPickers.swift b/submodules/LegacyMediaPickerUI/Sources/LegacyMediaPickers.swift
index 0000000..0000000 100644
--- a/submodules/LegacyMediaPickerUI/Sources/LegacyMediaPickers.swift
+++ b/submodules/LegacyMediaPickerUI/Sources/LegacyMediaPickers.swift
@@ -144,8 +144,10 @@ private final class LegacyAssetItemWrapper: NSObject {
     let forceHd: Bool
     let groupedId: Int64?
     let uniqueId: String?
+    // ClearGram: send this gallery video as a round video message.
+    let asRoundVideo: Bool
     
-    init(item: LegacyAssetItem, timer: Int?, spoiler: Bool?, price: Int64?, forceHd: Bool = false, groupedId: Int64?, uniqueId: String?) {
+    init(item: LegacyAssetItem, timer: Int?, spoiler: Bool?, price: Int64?, forceHd: Bool = false, groupedId: Int64?, uniqueId: String?, asRoundVideo: Bool = false) {
         self.item = item
         self.timer = timer
         self.spoiler = spoiler
@@ -153,12 +155,13 @@ private final class LegacyAssetItemWrapper: NSObject {
         self.forceHd = forceHd
         self.groupedId = groupedId
         self.uniqueId = uniqueId
+        self.asRoundVideo = asRoundVideo
         
         super.init()
     }
 }
 
-public func legacyAssetPickerItemGenerator() -> ((Any?, NSAttributedString?, String?, String?) -> [AnyHashable : Any]?) {
+public func legacyAssetPickerItemGenerator(asRoundVideo: Bool = false) -> ((Any?, NSAttributedString?, String?, String?) -> [AnyHashable : Any]?) {
     return { anyDict, caption, hash, uniqueId in
         let dict = anyDict as! NSDictionary
         let stickers = (dict["stickers"] as? [Data])?.compactMap { data -> FileMediaReference? in
@@ -248,13 +251,13 @@ public func legacyAssetPickerItemGenerator() -> ((Any?, NSAttributedString?, Str
             
             if let asset = dict["asset"] as? TGMediaAsset {
                 var result: [AnyHashable: Any] = [:]
-                result["item" as NSString] = LegacyAssetItemWrapper(item: .video(data: .asset(asset), thumbnail: thumbnail, cover: cover, adjustments: dict["adjustments"] as? TGVideoEditAdjustments, caption: caption, asFile: asFile, asAnimation: false, stickers: stickers), timer: (dict["timer"] as? NSNumber)?.intValue, spoiler: (dict["spoiler"] as? NSNumber)?.boolValue, price: price, groupedId: (dict["groupedId"] as? NSNumber)?.int64Value, uniqueId: uniqueId)
+                result["item" as NSString] = LegacyAssetItemWrapper(item: .video(data: .asset(asset), thumbnail: thumbnail, cover: cover, adjustments: dict["adjustments"] as? TGVideoEditAdjustments, caption: caption, asFile: asFile, asAnimation: false, stickers: stickers), timer: (dict["timer"] as? NSNumber)?.intValue, spoiler: (dict["spoiler"] as? NSNumber)?.boolValue, price: price, groupedId: (dict["groupedId"] as? NSNumber)?.int64Value, uniqueId: uniqueId, asRoundVideo: asRoundVideo)
                 return result
             } else if let url = (dict["url"] as? String) ?? (dict["url"] as? URL)?.absoluteString {
                 let dimensions = (dict["dimensions"]! as AnyObject).cgSizeValue!
                 let duration = (dict["duration"]! as AnyObject).doubleValue!
                 var result: [AnyHashable: Any] = [:]
-                result["item" as NSString] = LegacyAssetItemWrapper(item: .video(data: .tempFile(path: url, dimensions: dimensions, duration: duration), thumbnail: thumbnail, cover: cover, adjustments: dict["adjustments"] as? TGVideoEditAdjustments, caption: caption, asFile: asFile, asAnimation: false, stickers: stickers), timer: (dict["timer"] as? NSNumber)?.intValue, spoiler: (dict["spoiler"] as? NSNumber)?.boolValue, price: price, groupedId: (dict["groupedId"] as? NSNumber)?.int64Value, uniqueId: uniqueId)
+                result["item" as NSString] = LegacyAssetItemWrapper(item: .video(data: .tempFile(path: url, dimensions: dimensions, duration: duration), thumbnail: thumbnail, cover: cover, adjustments: dict["adjustments"] as? TGVideoEditAdjustments, caption: caption, asFile: asFile, asAnimation: false, stickers: stickers), timer: (dict["timer"] as? NSNumber)?.intValue, spoiler: (dict["spoiler"] as? NSNumber)?.boolValue, price: price, groupedId: (dict["groupedId"] as? NSNumber)?.int64Value, uniqueId: uniqueId, asRoundVideo: asRoundVideo)
                 return result
             }
         } else if (dict["type"] as! NSString) == "cameraVideo" {
@@ -807,6 +810,21 @@ public func legacyAssetPickerEnqueueMessages(
                                     break
                             }
                         case let .video(data, thumbnail, cover, adjustments, caption, asFile, asAnimation, stickers):
+                            // ClearGram: turn this into a round video message — centred square crop,
+                            // videoMessage conversion preset, 60s cap. The square h264 re-encode itself
+                            // happens in FetchVideoMediaResource when the resource is fetched for upload.
+                            var adjustments = adjustments
+                            let asRoundVideo = item.asRoundVideo && !asFile && !asAnimation && item.groupedId == nil
+                            if asRoundVideo {
+                                let originalSize: CGSize
+                                switch data {
+                                case let .asset(asset):
+                                    originalSize = asset.dimensions
+                                case let .tempFile(_, dimensions, _):
+                                    originalSize = dimensions
+                                }
+                                adjustments = clearRoundVideoAdjustments(originalSize: originalSize, existing: adjustments)
+                            }
                             var finalDimensions: CGSize
                             var finalDuration: Double
                             switch data {
@@ -838,8 +856,17 @@ public func legacyAssetPickerEnqueueMessages(
                                 finalDimensions = TGFitSize(finalDimensions, CGSize(width: 848.0, height: 848.0))
                             }*/
                             
+                            if asRoundVideo {
+                                finalDimensions = CGSize(width: clearRoundVideoSide, height: clearRoundVideoSide)
+                                finalDuration = min(finalDuration, clearRoundVideoMaxDuration)
+                            }
+                            
                             var previewRepresentations: [TelegramMediaImageRepresentation] = []
-                            if let thumbnail = thumbnail {
+                            if var thumbnail = thumbnail {
+                                if asRoundVideo {
+                                    // Keep the placeholder square or the circle renders squashed.
+                                    thumbnail = clearSquareCroppedImage(thumbnail)
+                                }
                                 let resource = LocalFileMediaResource(fileId: Int64.random(in: Int64.min ... Int64.max))
                                 let thumbnailSize = finalDimensions.aspectFitted(CGSize(width: 320.0, height: 320.0))
                                 let thumbnailImage = TGScaleImageToPixelSize(thumbnail, thumbnailSize)!
@@ -850,7 +877,7 @@ public func legacyAssetPickerEnqueueMessages(
                             }
                             
                             var videoCover: TelegramMediaImage?
-                            if let cover {
+                            if let cover, !asRoundVideo {
                                 let resource = LocalFileMediaResource(fileId: Int64.random(in: Int64.min ... Int64.max))
                                 let maxSide: CGFloat = 1280.0
                                 let coverSize = cover.size.aspectFitted(CGSize(width: maxSide, height: maxSide))
@@ -880,7 +907,7 @@ public func legacyAssetPickerEnqueueMessages(
                             if asAnimation {
                                 preset = TGMediaVideoConversionPresetAnimation
                             }
-                            if !asAnimation {
+                            if !asAnimation && !asRoundVideo {
                                 finalDimensions = TGMediaVideoConverter.dimensions(for: finalDimensions, adjustments: adjustments, preset: TGMediaVideoConversionPresetCompressedMedium)
                             }
                             
@@ -918,6 +945,10 @@ public func legacyAssetPickerEnqueueMessages(
                                     }
                             }
                             
+                            if asRoundVideo {
+                                fileName = "video.mp4"
+                            }
+                            
                             let estimatedSize = TGMediaVideoConverter.estimatedSize(for: preset, duration: finalDuration, hasAudio: !asAnimation)
                             
                             var fileAttributes: [TelegramMediaFileAttribute] = []
@@ -926,7 +957,10 @@ public func legacyAssetPickerEnqueueMessages(
                                 fileAttributes.append(.Animated)
                             }
                             if !asFile {
-                                let flags: TelegramMediaVideoFlags = [.supportsStreaming]
+                                var flags: TelegramMediaVideoFlags = [.supportsStreaming]
+                                if asRoundVideo {
+                                    flags.insert(.instantRoundVideo)
+                                }
                                 fileAttributes.append(.Video(duration: finalDuration, size: PixelDimensions(finalDimensions), flags: flags, preloadSize: nil, coverTime: nil, videoCodec: nil))
                                 if let adjustments = adjustments {
                                     if adjustments.sendAsGif {
diff --git a/submodules/MediaPickerUI/Sources/MediaPickerScreen.swift b/submodules/MediaPickerUI/Sources/MediaPickerScreen.swift
index 0000000..0000000 100644
--- a/submodules/MediaPickerUI/Sources/MediaPickerScreen.swift
+++ b/submodules/MediaPickerUI/Sources/MediaPickerScreen.swift
@@ -1468,7 +1468,7 @@ public final class MediaPickerScreenImpl: ViewController, MediaPickerScreen, Att
             }
         }
         
-        fileprivate func send(fromGallery: Bool = false, asFile: Bool = false, silently: Bool, scheduleTime: Int32?, animated: Bool, parameters: ChatSendMessageActionSheetController.SendParameters?, completion: @escaping () -> Void) {
+        fileprivate func send(fromGallery: Bool = false, asFile: Bool = false, asRoundVideo: Bool = false, silently: Bool, scheduleTime: Int32?, animated: Bool, parameters: ChatSendMessageActionSheetController.SendParameters?, completion: @escaping () -> Void) {
             guard let controller = self.controller, !controller.completed else {
                 return
             }
@@ -1500,9 +1500,9 @@ public final class MediaPickerScreenImpl: ViewController, MediaPickerScreen, Att
                 let signals: [Any]!
                 switch controller.subject {
                 case .assets:
-                    signals = TGMediaAssetsController.resultSignals(for: controller.interaction?.selectionState, editingContext: controller.interaction?.editingState, intent: asFile ? TGMediaAssetsControllerSendFileIntent : TGMediaAssetsControllerSendMediaIntent, currentItem: nil, storeAssets: true, convertToJpeg: convertToJpeg, descriptionGenerator: legacyAssetPickerItemGenerator(), saveEditedPhotos: controller.saveEditedPhotos)
+                    signals = TGMediaAssetsController.resultSignals(for: controller.interaction?.selectionState, editingContext: controller.interaction?.editingState, intent: asFile ? TGMediaAssetsControllerSendFileIntent : TGMediaAssetsControllerSendMediaIntent, currentItem: nil, storeAssets: true, convertToJpeg: convertToJpeg, descriptionGenerator: legacyAssetPickerItemGenerator(asRoundVideo: asRoundVideo), saveEditedPhotos: controller.saveEditedPhotos)
                 case .media:
-                    signals = TGMediaAssetsController.pasteboardResultSignals(for: controller.interaction?.selectionState, editingContext: controller.interaction?.editingState, intent: asFile ? TGMediaAssetsControllerSendFileIntent : TGMediaAssetsControllerSendMediaIntent, currentItem: nil, descriptionGenerator: legacyAssetPickerItemGenerator())
+                    signals = TGMediaAssetsController.pasteboardResultSignals(for: controller.interaction?.selectionState, editingContext: controller.interaction?.editingState, intent: asFile ? TGMediaAssetsControllerSendFileIntent : TGMediaAssetsControllerSendMediaIntent, currentItem: nil, descriptionGenerator: legacyAssetPickerItemGenerator(asRoundVideo: asRoundVideo))
                 }
                 guard let signals = signals else {
                     return
@@ -3146,6 +3146,25 @@ public final class MediaPickerScreenImpl: ViewController, MediaPickerScreen, Att
                             self?.controllerNode.send(asFile: false, silently: false, scheduleTime: nil, animated: true, parameters: nil, completion: {})
                         })))
                     }
+                    if ClearConfig.sendVideoAsCircle, price == nil, !hasSpoilers, clearCanSendAsRoundVideo(selectedItems: self.interaction?.selectionState?.selectedItems() ?? [], peer: self.peer) {
+                        items.append(.action(ContextMenuActionItem(text: ClearStrings.contextMenuSendAsVideoMessage, icon: { theme in
+                            return generateTintedImage(image: UIImage(bundleImageName: "Chat/Attach Menu/Video"), color: theme.contextMenu.primaryColor)
+                        }, action: { [weak self] _, f in
+                            f(.default)
+
+                            guard let self, let editingContext = self.interaction?.editingState,
+                                  let item = self.interaction?.selectionState?.selectedItems().first as? TGMediaEditableItem else {
+                                return
+                            }
+                            // Crop + trim first, then send: the editor writes its adjustments into
+                            // the picker's editing context, which the send path reads back.
+                            clearPresentRoundVideoEditor(context: self.context, item: item, editingContext: editingContext, present: { controller, arguments in
+                                self.present(controller, in: .window(.root), with: arguments)
+                            }, completed: { [weak self] in
+                                self?.controllerNode.send(asFile: false, asRoundVideo: true, silently: false, scheduleTime: nil, animated: true, parameters: nil, completion: {})
+                            })
+                        })))
+                    }
                     if selectionCount > 1, price == nil {
                         items.append(.action(ContextMenuActionItem(text: strings.Attachment_SendWithoutGrouping, icon: { theme in
                             return generateTintedImage(image: UIImage(bundleImageName: "Media Grid/GroupingOff"), color: theme.contextMenu.primaryColor)
