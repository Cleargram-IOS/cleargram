From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: keet <keetsta@icloud.com>
Date: Thu, 13 Aug 2026 23:13:11 +0800
Subject: Add configurable chat list swipe actions

---
 .../Sources/Node/ChatListItem.swift           | 24 ++++++++++++++++++-
 1 file changed, 23 insertions(+), 1 deletion(-)

diff --git a/submodules/ChatListUI/Sources/Node/ChatListItem.swift b/submodules/ChatListUI/Sources/Node/ChatListItem.swift
index 0000000..0000000 100644
--- a/submodules/ChatListUI/Sources/Node/ChatListItem.swift
+++ b/submodules/ChatListUI/Sources/Node/ChatListItem.swift
@@ -704,6 +704,18 @@ private enum RevealOptionKey: Int32 {
     case edit
 }
 
+// Symbol-based rather than literal, so an upstream reorder of the enum can't silently mis-map.
+private let clearSwipeActionKeyMap: [Int32: ClearSwipeAction] = [
+    RevealOptionKey.pin.rawValue: .pin,
+    RevealOptionKey.unpin.rawValue: .pin,
+    RevealOptionKey.mute.rawValue: .mute,
+    RevealOptionKey.unmute.rawValue: .mute,
+    RevealOptionKey.toggleMarkedUnread.rawValue: .read,
+    RevealOptionKey.delete.rawValue: .delete,
+    RevealOptionKey.archive.rawValue: .archive,
+    RevealOptionKey.unarchive.rawValue: .archive
+]
+
 private func canArchivePeer(id: EnginePeer.Id, accountPeerId: EnginePeer.Id) -> Bool {
     if id.isTelegramNotifications {
         return false
@@ -4045,7 +4057,17 @@ public class ChatListItemNode: ItemListRevealOptionsItemNode {
                     ItemListRevealOption(key: option.key, title: option.title, icon: .none, color: option.color, iconColor: option.iconColor, textColor: option.textColor)
                 }
             }
-            
+            if ClearSwipeActions.isConfigured,
+               case let .peer(clearPeerData) = item.content,
+               clearPeerData.customMessageListData == nil,
+               case .chatList = item.chatListLocation,
+               promoInfo == nil,
+               !item.editing {
+                let clearSwipe = ClearSwipeActions.arrange(left: peerLeftRevealOptions, right: peerRevealOptions, family: clearSwipeActionKeyMap)
+                peerLeftRevealOptions = clearSwipe.left
+                peerRevealOptions = clearSwipe.right
+            }
+
             let (onlineLayout, onlineApply) = onlineLayout(online, onlineIsVoiceChat)
             var animateContent = false
             if let currentItem = currentItem, currentItem.content.chatLocation == item.content.chatLocation {
